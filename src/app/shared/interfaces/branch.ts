export interface Branch {
    description: string;
    address: string;
    createdAt: string;
    isActive: boolean;
}
export interface newBranch {
    description: string;
    address: string;
    numDni: string;
}